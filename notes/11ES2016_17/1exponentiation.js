/*
    Exponentiation
        - repacement of Math.pow(num, power of);
*/

    // Example A:
        2 ** 4; // 2 to the power of 4; result 16
    
    // Example B:
        var arr = [1,2,3,4];
        var total = 2;
        arr.map(val => total **= val);
 